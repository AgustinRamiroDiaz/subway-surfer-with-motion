[project]
name = "pose-estimation-tracker-server"
version = "0.1.0"
description = "WebRTC pose tracker for the Subway Surfer motion UI"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aiortc>=1.14.0",
    "numpy>=2.2.0",
    "opencv-python>=4.13.0.92",
    "ultralytics>=8.4.51",
    "websockets>=15.0.1",
]

[project.scripts]
pose-tracker-server = "pose_estimation_tracker_server.server:main"
pose-tracker-preview = "pose_estimation_tracker_server.preview:main"

[dependency-groups]
dev = [
    "mypy>=1.18.0",
    "pytest>=9.0.0",
    "ruff>=0.12.0",
]

[tool.ruff]
line-length = 120
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.mypy]
python_version = "3.12"
check_untyped_defs = true
disallow_untyped_defs = true
ignore_missing_imports = true
warn_redundant_casts = true
warn_unused_ignores = true

[build-system]
requires = ["uv_build>=0.11.7,<0.12.0"]
build-backend = "uv_build"
