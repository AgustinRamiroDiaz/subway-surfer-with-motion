{
  "name": "subway-surfer-with-motion",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.1.2",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "prepare": "husky",
    "start": "pnpm --filter @subway-surfer-with-motion/frontend start",
    "build": "pnpm --filter @subway-surfer-with-motion/frontend build",
    "test": "pnpm --filter @subway-surfer-with-motion/frontend test",
    "test:e2e": "pnpm --filter @subway-surfer-with-motion/frontend test:e2e",
    "test:python": "uv run --project pose-estimation-tracker-server pytest",
    "lint": "pnpm --filter @subway-surfer-with-motion/frontend lint",
    "lint:python": "uv run --project pose-estimation-tracker-server ruff check pose-estimation-tracker-server/src pose-estimation-tracker-server/tests && uv run --project pose-estimation-tracker-server mypy pose-estimation-tracker-server/src",
    "verify": "pnpm lint && pnpm test && pnpm build && pnpm lint:python && pnpm test:python",
    "lint:fix": "pnpm --filter @subway-surfer-with-motion/frontend lint:fix",
    "profile:e2e": "node scripts/profile-e2e.mjs"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  }
}
